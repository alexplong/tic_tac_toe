const player = (name) => {

  const playerName = name;

  // cache DOM

  const board = document.querySelector('.gameboard-container');
  const square = document.querySelectorAll('.square');


  // public methods
  
  const selectPlayer = () => {
      
  }
  
  const makeMove = () => {
    
  }

  return {selectPlayer, makeMove}
};


// const alex = player('alex')
// alex.getMoves()




